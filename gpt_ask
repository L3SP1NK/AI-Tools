#!/bin/bash
set -euxo pipefail

declare -r dmenucmd='rofi -dmenu'

question=$(${dmenucmd} -p "🤖 Ask ChatGPT:")
response=$(echo "${question}" | gpt_prompt || { notify-send "No response from ChatGPT!"; exit 1; })

notify-send "${response}"
