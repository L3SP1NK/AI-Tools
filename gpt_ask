#!/bin/bash
question=$(rofi -dmenu -p "🤖 Ask ChatGPT:")

if [ -z "${question}" ]; then
	exit 1
fi

response=$(echo "${question}"|gpt_prompt)

if [ -z "${response}" ]; then
	exit 1
fi

notify-send "${response}"
